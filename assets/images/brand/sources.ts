/** Local relative requires — Metro resolves these reliably (aliases do not). */
export const brandImageSources = {
  logo: require('./gunuco-logo.jpg'),
  taglineGiraffes: require('./home-tagline-giraffes.jpg'),
} as const;
