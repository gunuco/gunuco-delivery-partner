/** Local relative requires - Metro resolves these reliably (aliases do not). */
export const earningsImageSources = {
  banner: require('./banner-earnings.jpg'),
  heroCard: require('./hero-card.jpg'),
} as const;
